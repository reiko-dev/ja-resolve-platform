# MUSE — MVP-04 Adversarial Review (fresh, frozen tree)

Reviewer: MUSE (adversarial; executor is a different model — nothing trusted, everything re-verified).
Reviewed SHA: `7eec7e57` (detached worktree; branch `feature/mvp-04-proposals-assignment` points here).
Implementation SHA: `8b550440`. Execution base: `7ad3e56b` (ancestor — confirmed).
Date (UTC): 2026-09-20. Method: code read at the frozen tree + every gate re-run by the reviewer.

## 0. Frozen-tree verification — PASS

- `git diff --stat 8b550440..7eec7e57` touches **17 files, all under `docs/`** (+4243/−0
  functional delta is zero). `git diff --name-only … | grep -v '^docs/'` returns empty:
  **no functional file changed after the implementation head.** P1 condition absent.
- `git status --short` is clean before and after this review (the only transient dirt was
  `docs/evidence/t01/db-baseline-gate.json`, regenerated by my own baseline run; it differed
  **only** in the disposable project hash and the random seed admin e-mail, fingerprint
  identical, and was restored via `git checkout --`).
- `git diff 7ad3e56b..8b550440 --stat`: 48 files, +6641/−148. Every non-docs path falls in
  the MVP-04 surface (tow module, migration `005`, `tests/tow/mvp04/`, baseline re-pin,
  contract draft.6, two narrowed MVP-03 absence assertions, one test helper). No `006`
  migration, no payment/counteroffer/tracking paths, no legacy-model coupling.

## 1. Proposal authority — PASS (plus live proof)

- Caller price/route/tariff/partner/vehicle rejected, not ignored:
  `domain/tow-proposal.js:70-88` (`FORBIDDEN_PROPOSAL_INPUT_KEYS`, 18 keys) enforced by
  `validateCreateTowProposalInput` (`:136-152`); any non-empty body is `422`. The route
  declares no body schema bypass: `http/routes.js:50` → `proposal-controller.js:22-30`
  passes raw `req.body` straight to the service, which validates before any DB/provider touch.
- Authenticated partner + its actual active vehicle used:
  `application/proposal-service.js:136-137` (`partnerRepository.findById(partnerId)` from the
  token, `vehicleRepository.findActiveByPartner`); identity comes from
  `req.user.partner_id` (`proposal-controller.js:25`), never body/query.
- Eligibility revalidated with accepted MVP-03 semantics, no hidden tightening:
  `proposal-service.js:161-168` calls `evaluateTowMatch` (the same pure function the
  opportunity feed uses, `matching-service.js:88`) and maps every code through without
  adding `is_verified`/`approval_status` — grep over `domain/` finds those strings nowhere
  near eligibility (only `documents.js:6` mentions an approval expiry in another context).
  A dedicated test pins an unverified partner may still propose
  (`towProposalCreate.test.js:265`).
- Price from MVP-02 quote authority: `proposal-service.js:191-200` calls
  `quoteService.quoteTow` with provider position + frozen endpoints + stored tariff — the
  first and only provider call (cheap checks precede it), and replay returns the stored row
  without re-pricing (`:139-158`).
- Immutable snapshot — **proven live by the reviewer** (inline harness script, SQLite,
  real composition): created a proposal, then mutated the vehicle tariff
  (`minimum_charge_cents=999999`), the vehicle plate, the partner coordinates AND the
  `tow_proposal_expiry_minutes` setting — re-read proposal shows `price_unchanged: true`,
  `route_unchanged: true`, `vehicle_snapshot_unchanged: true`. Accept then shows
  `final_price == frozen price` with **zero additional provider calls**
  (`routeProvider.callCount()` unchanged across accept). `assignment-service.js` does not
  even receive `quoteService` (composition wires module/settings/repos/unitOfWork/clock only),
  so re-quoting at accept is structurally impossible; `buildAssignmentRecord` copies
  `locked.price_amount_cents` (`assignment-service.js:107-117`).
- Invented-field audit: every key in `FORBIDDEN_PROPOSAL_INPUT_KEYS` (price, amount,
  tariff, pricing, partner_id, tow_vehicle_id, status, expires_at, …) is refused. PASS.

## 2. Expiry — PASS

- `expires_at = now + tow_proposal_expiry_minutes` snapshotted on the row
  (`proposal-service.js:202`); `expires_at > created_at` is a DB CHECK
  (`005:211`). Boundary `now >= expires_at` (`tow-proposal.js:461-467`).
- Action-time only: `assertProposalActionable` (`tow-proposal.js:475-483`) is called by
  accept (`assignment-service.js:105`) and withdraw (`proposal-service.js:312`). Repo-wide
  grep for `schedule|cron|node-cron|setInterval|setTimeout` under `src/modules/tow`
  returns **only comments stating no scheduler exists**. Expired proposals keep row status
  `ACTIVE` and are refused with `409 proposal_expired`; an expired proposal can never be
  accepted (actionable gate runs against the locked row) nor withdrawn as ACTIVE
  (withdraw calls the same gate). Listing filters by the contract enum without rewriting
  rows. `EXPIRED` is never written by the runtime (only `ACCEPTED`/`WITHDRAWN`/`CLOSED`
  appear in writers — verified by grep). Truthful, as disclosed.

## 3. Atomic assignment — PASS

- One transaction: `unitOfWork = { run: (fn) => db.transaction(fn) }`
  (`composition.js:68`); `accept` binds all three repositories to `trx`
  (`assignment-service.js:155-159`).
- Row locks: request `lockById` (`:87`, `SELECT … FOR UPDATE` on PG via
  `adapters/persistence/row-lock.js:30-32`, documented no-op on SQLite) and proposal
  `lockById` (`:104`). The P1 stale-snapshot fix is real: every guard, `markAssigned`,
  `closeActiveForRequestExcept` and the response use `lockedRequest`, never the pre-lock
  snapshot (`:95-144`).
- DB uniqueness is the authority, no pre-checks: `createForProposal`
  (`assignment-repository.js:165-194`) inserts blind and disambiguates by constraint name
  (`error.constraint` on PG; constraint-part of message only — the code comments explain
  why whole-message matching would misclassify). Four authorities: `UNIQUE(tow_request_id)`
  (one assignment per request, ever), `UNIQUE(proposal_id)` (accept idempotency),
  partial `WHERE released_at IS NULL` per-partner and per-vehicle occupancy
  (`005:266-275`). Losers → `CLOSED` (`closeActiveForRequestExcept` predicates
  `status='ACTIVE'` + excludes winner), winner → `ACCEPTED`, request → `ASSIGNED`
  (`assignment-service.js:140-142`). Replay-before-guards (`:92-93`) returns the committed
  assignment as `200`. All confirmed in code AND under concurrency (see §10).

## 4. Multiple proposals — PASS

- First proposal moves `SEARCHING → NEGOTIATING` via SQL-guarded `markNegotiating`
  (`proposal-service.js:253-255`); concurrent second partner changes 0 rows (idempotent by
  construction). Matchable states are exactly `SEARCHING` + `NEGOTIATING`
  (`domain/tow-request.js:62`, `OPEN_TOW_REQUEST_STATES`); assigned requests answer
  `409 request_already_assigned` on create (`proposal-service.js:171-177`) and accept
  (`assignment-service.js:95-97`) and are never reopened (no transition out of `ASSIGNED`
  exists in `tow-request-repository.js`). Live test pins simultaneous ACTIVE proposals from
  multiple partners (`towProposalCreate.test.js:135`).

## 5. State — PASS

- Only `SEARCHING → NEGOTIATING → ASSIGNED` reachable: writers are `markNegotiating`,
  `markAssigned` (request) and `markAccepted`/`markWithdrawn`/`closeActiveForRequestExcept`
  (proposals → `ACCEPTED`/`WITHDRAWN`/`CLOSED`). `COUNTERED` appears solely in the frozen
  enum (`tow-proposal.js:44`) and the migration CHECK (`005:58-66`); **no runtime write of
  `COUNTERED`, `REJECTED` or `EXPIRED` exists** (grep over writers confirms). No MVP-05
  operational states/behavior; architecture suite pins it
  (`towMvp04Architecture.test.js:277-297`).

## 6. Rehydration — PASS

- Customer `GET /requests/:id` hydrates `assignment` (via `findByRequestId`) and truthful
  `allowed_actions` (`tow-request-service.js:128-139`, batched for lists `:160-169`);
  `allowedActionsForRequest` returns `['ACCEPT_PROPOSAL']` only when open AND a live proposal
  exists, else `[]` (`tow-request.js:133-134`); after accept it is `[]`
  (`assignment-service.js:165`). My live proof confirmed `allowed_actions: []` post-accept
  and assignment keys limited to the contract shape.
- Proposal lists: customer list is owner-only (`403 not_request_owner`,
  `proposal-service.js:260-282`, reports `request_state`); partner book uses token identity
  only (`:284-298`); both paginated + status-filtered against the contract enum
  (`parseStatusFilter`, `validateListQuery`). No over-claimed actions.

## 7. Scope — PASS

- Exactly five MVP-04 routes (`http/routes.js:50-54`); counteroffer/tracking/payments/MVP-05
  unrouted (architecture test bans `/counteroffer /assignment /cancel /destination
  /completion /dispute /review /payments …`). Counteroffer: enum member + `counteroffer:
  null` DTO only, no runtime. #31/#33/VPS untouched (file list proves it). Migrations
  exactly `001..005` (baseline gate pins the list; `ls` shows no `006`; smuggled-`006`
  negative control exists at `dbBaselineSafety.test.js:675-677`). Legacy
  `EmergencyRequest`/`tow_proposals` appear in canonical modules only in comments/audit
  text — no import, no query, no FK (migration touches only the two new tables; arch test
  asserts `dropTableIfExists` args and no `alterTable`).

## 8. Contract + baseline — PASS

- draft.6 explicit/documented/tested: canonical version `1.0.0-draft.6`
  (`tow-api-contract.openapi.yaml:4`, revision note `:19`); ownership move (4 path items
  inlined, 5 components added) is shape-preserving — reviewer's `validate:openapi` run
  shows `dropped=[]` on **every** path and an empty allowlist. `proposal_already_active`
  present in both documents' `ErrorResponse.error.code` (canonical `:568`, base `:1833`,
  lists byte-equal) AND in the validator's `CANONICAL_ERROR_CODES`
  (`tests/helpers/towContract.js:75`) — docs and validator agree. Version pins moved with
  the document (`openapi.structure.test.js`, `towPartnerOpportunitiesContract.test.js`).
  Base keeps `1.0.0-draft.2`, no new paths/schemas.
- Baseline fingerprint `efb6158bf9df52434f8e21bfa6d15a98fa9a5ee02a0ca0ee13144dc9c27d9cdc`
  reproduced deterministically on two fresh runs by the reviewer (35 tables / 33 rows /
  settings 25, migrations `001..005`). Smuggled-`006` RED control present and meaningful.

## 9. Gates — PASS (two pre-existing-flake reruns, both resolved GREEN)

All run by the reviewer on the frozen tree. Note: the environment holds a foreign container
(`akry-edge-pg`) on the default PG port 55432 (not this project's; left untouched), so all
PG legs ran with `DB_PORT=55434` as the mandate prescribes.

| Command | Result |
| --- | --- |
| `npm run validate:openapi` | PASS, exit 0 — 0 enum mismatches, `dropped=[]` all paths, allowlist empty, draft.6 |
| `npm run test:contract` | PASS — 5 suites / 62 tests |
| `DB_PORT=55434 npm run verify:tow` | GREEN — OpenAPI PASS; contract 62/62; offline tow 1011 passed / 75 skipped / 1086; PG foundation 6/6. First attempt: 1 unrelated offline failure (flake family, name not captured); immediate rerun GREEN |
| `npx jest tests/tow/mvp01` | 13 passed + 2 PG-skipped suites; 124 passed / 10 skipped / 134 |
| `npx jest tests/tow/mvp02` | 5/5 suites; 204/204 |
| `npx jest tests/tow/mvp03` | 8/8 suites; 177 passed / 7 skipped / 184 |
| `npx jest tests/tow/mvp04` | 7/7 suites; **155 passed / 10 skipped / 165** (exactly the claimed counts) |
| `npx jest tests/tow` | 49 passed + 5 skipped suites; 1011 passed / 75 skipped / 1086, 0 failures |
| `DB_PORT=55434 npm run test:db-baseline` | GREEN — 35 tables / 33 rows / settings 25; `efb6158b…` identical on both runs; `001..005` pinned |
| `npx jest --runInBand` (full) | First run: 1 failure in legacy `tests/integration/deliveryOrders.test.js` (transport-flake family; that suite standalone: 49/49 PASS). Rerun: **GREEN — 82 suites / 1435 passed / 75 skipped / 1510, 0 failures** |
| `DB_PORT=55434 npm run test:pg:up/wait` | healthy |
| `TOW_POSTGRES_E2E=1 … towMvp04Postgres.e2e` | **10/10 PASS** (DB_PORT=55434; without it the suite correctly fails with auth error against the foreign 55432 container — harness isolation confirmed, not a product issue) |
| `… dbBaseline.e2e` | 33/33 PASS |
| `… towMvp03Postgres.e2e` | 7/7 PASS |
| `… towPostgres.e2e + g3TowPostgres.e2e` | 9/9 PASS |
| `npm run test:pg:down` | 0 harness containers / 0 harness volumes / 0 harness networks |

No business failure waived. PG concurrency failures: none — the only failures seen were the
documented Supertest transport family on non-MVP-04 suites, each resolved by rerun.

## 10. Concurrency proof — PASS (C1/C4/C5 reproduced by the reviewer)

- Full matrix re-run by reviewer: 10/10 GREEN on real PostgreSQL.
- Isolation re-runs: C1 (two proposals/one request → exactly one assignment, loser `409
  request_already_assigned`), C4 (partner occupancy via partial unique index), C5
  (disable×accept serialized) — each 1 passed / 9 skipped in isolation. DB (23505 /
  partial unique indexes) is the authority: `createForProposal` inserts without pre-read
  and classifies by constraint; C3 proves `UNIQUE(tow_request_id)` rejects a raw second
  assignment with 23505.
- Executor-claimed defects verified real in code: P1 (locked-row usage,
  `assignment-service.js:87-144`), P2 (sibling-key disambiguation by constraint name +
  row-existence fallback, `tow-proposal-repository.js:360-386`), P3 (ISO-instant binding,
  `toColumns`/`findLiveRequestIds`), D1 (C3 carries the NOT NULL column). C9 determinism
  mechanism (`pg_stat_activity` Lock wait) read and plausible; NC-6b RED history consistent
  with it. Regression tests are meaningful: each pins the exact fixed behavior (loser code
  C1, no-500 C6, window filter `07-timestamp-binding.md` scope).

## 11. Negative controls — PASS (NC-3 + NC-8 reproduced by the reviewer)

Backups under `muse-mvp04-nc/` (repo-adjacent, never `/tmp`). SHA-256 before == after for both.

- **NC-MVP04-3** (stale snapshot; `assignment-service.js`): RED `4 failed / 18 passed / 22`
  — exactly the executor's counts — restore hash `c7e6171a…` identical, GREEN `22/22`.
- **NC-MVP04-8** (drop `UNIQUE(tow_request_id)`; migration `005`): RED `2 failed / 8 passed /
  10` (migration-authority + C3) — exactly the executor's counts — restore hash
  `6c67a38f…` identical, GREEN `10/10`.
- The remaining 7 controls' mutations were read in the driver; RED/GREEN counts in
  `17-negative-controls.md` are internally consistent with the suites' assertion sets.
  NC-6b's GREEN-then-RED history (test defect → C9 fix) is coherent and C9 now passes.

## 12. No weakened tests — PASS (narrowing verified at-least-as-strong)

- `git diff` grep for `.only/.skip/xit/xtest/xdescribe`: only the pre-existing opt-in
  `describe.skip` PG-gating pattern (unchanged semantics). No removed assertions except the
  disclosed set: version pins (draft.5→6, mechanical), migration pin (001-004→001-005,
  additive), and the two narrowed MVP-03 absence assertions.
- Narrowing audit: banned-route list dropped `/proposals`+`/accept` but keeps
  `/counteroffer /assignment /cancel /destination /completion /dispute /review /payments
  /partner/status /partner/location`; banned-table regex dropped `tow_assignments` but keeps
  legacy `tow_proposals` with quote-anchoring so canonical `tow_request_proposals` cannot
  match. Replacements: `towMvp04Architecture.test.js` (29 tests) asserts exactly the five
  MVP-04 routes, module-gate-first on every write path, no MVP-05 table/route/vocabulary
  leakage, `COUNTERED` never written, DTO/contract vocabulary agreement — strictly stronger
  coverage of the moved surfaces than the old absence assertions. No MVP-03 behavior
  assertion weakened (MVP-03 suites green: 177 passed).

## Claims audited — all verified, none falsified

`8b550440` = 48 files +6641/−148 (verified) · docs-only follow-ups (verified, P1 absent) ·
server-priced create order of 10 steps (verified against `proposal-service.js:118-258`) ·
frozen `expires_at` + no scheduler (verified) · one-transaction accept + row locks
(verified) · DB-as-authority ×4 constraints (verified live) · C1–C9 matrix (re-run 10/10) ·
P1/P2/P3/D1 fixes (verified in code) · 9/9 NC RED→GREEN (2 reproduced) · counts
155/10/165, 1011/75/1086, 1435/75/1510, 62/62, 35 tables, `efb6158b…`×2 (all reproduced) ·
flake census (consistent with reviewer's own 2 flake observations, both resolved GREEN).

## Findings

- P0: 0. P1: 0. Blocking P2: 0.
- P3 (documented, non-blocking, all already disclosed in the work result): pre-existing
  Supertest transport flake (observed twice by reviewer, both resolved GREEN on rerun;
  harness-owned, not MVP-04); lazy expiry (rows stay `ACTIVE` until acted on — truthful,
  no scheduler by design); `COUNTERED`/`REJECTED`/`EXPIRED` unreachable by design;
  occupancy answered with generic `conflict` + `details.reason` (contract has no
  `partner_busy` code); `released_at` always NULL (no release path yet — MVP-05+);
  `is_verified`/`approval_status` do not gate proposals (accepted MVP-03 semantics,
  CONTRACT_CONFLICT-1); legacy `models/Partner.js` stale columns (pre-existing, untouched);
  no lint/typecheck script (pre-existing); MVP-03 absence assertions narrowed with
  stronger replacements (verified above).

# MUSE — MVP-04 Adversarial Review
Verdict: APPROVE_WITH_FINDINGS
Reviewed SHA: 7eec7e57
Implementation SHA: 8b550440
P0: 0
P1: 0
blocking P2: 0
P3: transport-flake (pre-existing, harness-owned); lazy-expiry truthfulness; COUNTERED/REJECTED/EXPIRED unreachable by design; generic-conflict for occupancy; released_at always NULL; is_verified non-gating (accepted MVP-03 semantics); legacy Partner.js stale columns; no lint script — all disclosed/non-blocking
Checks: 1 PASS, 2 PASS, 3 PASS, 4 PASS, 5 PASS, 6 PASS, 7 PASS, 8 PASS, 9 PASS (2 flake reruns → GREEN), 10 PASS (C1/C4/C5 reproduced), 11 PASS (NC-3 + NC-8 reproduced), 12 PASS
Claims audited: all verified / none falsified
Live gates: validate:openapi PASS; test:contract 62/62; verify:tow GREEN (1011/75/1086 + PG foundation 6/6); mvp01 124/134; mvp02 204/204; mvp03 177/184; mvp04 155/10/165; tests/tow 1011/75/1086; test:db-baseline GREEN efb6158b x2; full jest GREEN 1435/75/1510 (1 legacy flake → rerun GREEN); PG mvp04 10/10; PG baseline 33/33; PG mvp03 7/7; PG legacy 9/9; teardown 0/0/0
Concurrency: C1/C4/C5 reproduced (isolated + full 10/10); DB (23505/partial indexes) is the authority
Scope: counteroffer NO | MVP-05 NO | #33 NO | #31 NO | VPS NO
