FAIL tests/tow/foundation/towHarnessEnvFile.test.js
  T00 harness env file (Codex finding C1)
    loader contract
      ✓ applies the file values into the provided environment (2 ms)
      ✓ shell/CI values always win (never override:true) (1 ms)
      ✓ an empty value counts as unset (it cannot silently configure the target)
      ✓ a missing file is not an error: the guard defaults apply (1 ms)
      ✓ resolves the canonical path and honours TOW_TEST_ENV_FILE (1 ms)
      ✓ describeEnvFile never prints values (1 ms)
    the real entrypoints observe .env.test (child processes)
      ✕ pg-guard CLI loads .env.test and reports the resolved target (55 ms)
      ✕ the Knex helper resolves the file target and exports it to the app (181 ms)
      ✓ shell DB_PORT beats the file, end to end (58 ms)
      ✓ a shell TOW_POSTGRES_E2E=0 vetoes the file opt-in (62 ms)
      ✕ without a file the documented defaults still apply (no Docker needed) (61 ms)

  ● T00 harness env file (Codex finding C1) › the real entrypoints observe .env.test (child processes) › pg-guard CLI loads .env.test and reports the resolved target

    expect(received).toContain(expected) // indexOf

    Expected substring: "/var/folders/tj/v4pyr7mx54j09ps94lqs8qyh0000gn/T/tow-env-file-bomrdt/.env.test"
    Received string:    "Tow PostgreSQL test target: postgresql://tow_test@127.0.0.1:55432/socorre_ai_tow_test
    opt-in (TOW_POSTGRES_E2E=1): no
    "

      178 |
      179 |       expect(result.status).toBe(0);
    > 180 |       expect(result.stdout).toContain(file);
          |                             ^
      181 |       expect(result.stdout).toContain('postgresql://tow_test@127.0.0.1:55432/socorre_ai_tow_test');
      182 |       expect(result.stdout).toContain('SAFE');
      183 |       // The decoy `.env` in the working directory was never read.

      at Object.toContain (tests/tow/foundation/towHarnessEnvFile.test.js:180:29)

  ● T00 harness env file (Codex finding C1) › the real entrypoints observe .env.test (child processes) › the Knex helper resolves the file target and exports it to the app

    expect(received).toBe(expected) // Object.is equality

    Expected: "55432"
    Received: undefined

      220 |       expect(payload.target.user).toBe('tow_test');
      221 |       // The canonical target is made explicit for `src/config/database.js`.
    > 222 |       expect(payload.env.DB_PORT).toBe('55432');
          |                                   ^
      223 |       expect(payload.env.DB_NAME_TEST).toBe('socorre_ai_tow_test');
      224 |       expect(payload.env.DB_HOST).toBe('127.0.0.1');
      225 |       expect(payload.env.DB_USER).toBe('tow_test');

      at Object.toBe (tests/tow/foundation/towHarnessEnvFile.test.js:222:35)

  ● T00 harness env file (Codex finding C1) › the real entrypoints observe .env.test (child processes) › without a file the documented defaults still apply (no Docker needed)

    expect(received).toContain(expected) // indexOf

    Expected substring: "not present"
    Received string:    "Tow PostgreSQL test target: postgresql://tow_test@127.0.0.1:55432/socorre_ai_tow_test
    opt-in (TOW_POSTGRES_E2E=1): yes
    SAFE: target satisfies the disposable test environment rules
    "

      265 |
      266 |       expect(result.status).toBe(0);
    > 267 |       expect(result.stdout).toContain('not present');
          |                             ^
      268 |       expect(result.stdout).toContain('postgresql://tow_test@127.0.0.1:55432/socorre_ai_tow_test');
      269 |       expect(result.stdout).toContain('SAFE');
      270 |     });

      at Object.toContain (tests/tow/foundation/towHarnessEnvFile.test.js:267:29)

Test Suites: 1 failed, 1 total
Tests:       3 failed, 8 passed, 11 total
Snapshots:   0 total
Time:        0.781 s
Ran all test suites matching /tests\/tow\/foundation\/towHarnessEnvFile.test.js/i.
