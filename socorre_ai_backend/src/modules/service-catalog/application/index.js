/**
 * PLATFORM SERVICE CATALOG — application barrel.
 */
'use strict';

const { createCatalogService, toPublicItem, toAdminItem } = require('./catalog-service');

module.exports = { createCatalogService, toPublicItem, toAdminItem };
